COMPILATION METHOD

After cloning the repository, on the command line, `cd` into the game directory and run `mvn package`.
This creates a target folder that includes a fat jar file.

To run it type `java -jar target/JARFILE`
